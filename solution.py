MOD = 1,000,000,000
N = 입력받은 계단의 길이

// 1. 3차원 DP 배열 초기화: dp[현재_길이][마지막_숫자][방문_비트마스크]
// 크기는 [N + 1][10][1024] 이며 모두 0으로 채움
dp = 새로운 3차원 배열 생성

// 2. 초기값 세팅 (길이가 1일 때)
// 계단 수는 0으로 시작할 수 없으므로 1부터 9까지만 초기화
반복문 (i는 1부터 9까지):
    초기_마스크 = 1 << i  // i번째 비트만 1로 켬
    dp[1][i][초기_마스크] = 1

// 3. DP 테이블 채우기 (Push 방식: 현재 상태에서 다음 상태를 업데이트)
반복문 (현재_길이 는 1부터 N-1까지):
    반복문 (마지막_숫자 는 0부터 9까지):
        반복문 (현재_마스크 는 0부터 1023까지):
            
            현재_경우의수 = dp[현재_길이][마지막_숫자][현재_마스크]
            
            // 유효한 경우의 수가 없다면 계산할 필요 없음
            만약 현재_경우의수 == 0 이라면: 
                건너뛰기 (continue)

            // 방향 1: 다음 계단이 한 칸 낮아지는 경우 (-1)
            만약 마지막_숫자 > 0:
                다음_숫자 = 마지막_숫자 - 1
                // 기존 마스크에 다음 숫자의 비트를 OR 연산(|)하여 추가
                다음_마스크 = 현재_마스크 | (1 << 다음_숫자) 
                
                dp[현재_길이 + 1][다음_숫자][다음_마스크] = 
                    (dp[현재_길이 + 1][다음_숫자][다음_마스크] + 현재_경우의수) % MOD

            // 방향 2: 다음 계단이 한 칸 높아지는 경우 (+1)
            만약 마지막_숫자 < 9:
                다음_숫자 = 마지막_숫자 + 1
                // 기존 마스크에 다음 숫자의 비트를 OR 연산(|)하여 추가
                다음_마스크 = 현재_마스크 | (1 << 다음_숫자)
                
                dp[현재_길이 + 1][다음_숫자][다음_마스크] = 
                    (dp[현재_길이 + 1][다음_숫자][다음_마스크] + 현재_경우의수) % MOD

// 4. 정답 도출
정답 = 0
// 목표 길이 N에 도달했고, 모든 숫자(0~9)를 방문한 마스크(1023)인 경우를 모두 합산
반복문 (i는 0부터 9까지):
    정답 = (정답 + dp[N][i][1023]) % MOD

출력(정답)